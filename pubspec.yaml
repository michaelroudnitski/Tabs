name: tabs
description: A Flutter app made to manage debts with friends.

version: 0.0.1+1

environment:
  sdk: ">=2.5.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:
  firebase_core:
  # firebase_auth:
  cloud_firestore:
  provider:
  permission_handler:
  contacts_service:
  flutter_typeahead:
  flutter_money_formatter:

dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:
  uses-material-design: true
